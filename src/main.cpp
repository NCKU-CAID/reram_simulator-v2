#include<iostream>
#include <bitset>
#include<cstdlib>
#include "parser.h"

FILE *fptr
int main(int argc, char* argv[]){
	if(argc!=3){
		printf("usage: %s instruction_file SRAM_file",argv[0])
		return 1;
	}
	double instruction;
	while(fscanf(fptr,"%lld",&instruction)!=EOF){
		
	}
}
